[out:json][timeout:180][bbox:18.534,73.772,18.570,73.810];
(
  way[highway]; way[building]; relation[building];
  way[landuse]; relation[landuse]; way[natural]; relation[natural];
  way[leisure]; relation[leisure]; way[waterway]; way[railway]; way[barrier];
  node[place]; node[natural=tree]; node[highway]; node[crossing];
  relation[type=restriction];
);
(._;>;);
out body;
